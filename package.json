{
  "name": "market-ai",
  "private": true,
  "scripts": {
    "dev": "npm run start:dev",
    "start:dev": "concurrently -n auth,cart,client -c blue,green,magenta \"npm run dev:auth\" \"npm run dev:cart\" \"npm run dev:client\"",
    "start:dev:backend": "concurrently -n auth,cart -c blue,green \"npm run dev:auth\" \"npm run dev:cart\"",
    "dev:auth": "npm run start:dev --prefix services/auth-service",
    "dev:cart": "npm run start:dev --prefix services/cart-service",
    "dev:client": "npm run dev --prefix apps/client"
  },
  "devDependencies": {
    "concurrently": "^10.0.0"
  }
}
